# Personal tooling: local settings
.claude/settings.local.json
.claude/settings.json
.claude/.wr-canon

# Local credentials — Tidbyt API token and device ID. Never commit.
# .env.example is the checked-in template; creds.txt is the pre-.env filename.
.env
.env.local
creds.txt

# Rendered output, except the README preview image which is checked in.
*.webp
!capmetro.webp
