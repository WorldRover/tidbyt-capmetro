# Copy to .env and fill in. .env is gitignored — never commit real values.
# Both values come from the Tidbyt mobile app: Settings -> Developer.

# Tidbyt API token (a JWT). Treat it like a password.
TIDBYT_API_TOKEN=

# Target device ID, e.g. "quiet-oak-cloud-abc".
TIDBYT_DEVICE_ID=
